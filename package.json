{
  "name": "chat-fe",
  "version": "1.0.0",
  "type": "module",
 "scripts": {
  "build": "esbuild src/app.ts --bundle --outfile=dist/app.js",
  "start": "npx serve .",
  "dev": "esbuild src/app.ts --bundle --outfile=dist/app.js && npx serve ."
},
  "devDependencies": {
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@a2ui/lit": "file:./renderers/lit",
    "@lit-labs/signals": "^0.2.0",
    "@lit/context": "^1.1.6",
    "lit": "^3.3.2",
    "signal-polyfill": "^0.2.2"
  }
}
